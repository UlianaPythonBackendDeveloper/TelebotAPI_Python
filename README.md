# Telegram Bot

## Description
Simple Telegram bot

## Installation
1. Clone the repository
2. Install dependencies: `pip install pyTelegramBotAPI`
3. Create a `.env` file with your token: `TELEGRAM_BOT_TOKEN=your_token`
4. Run: `python bot.py`

## IMPORTANT
Do not upload the token to a public repository!